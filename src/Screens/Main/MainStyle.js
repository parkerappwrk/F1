import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    fullScreen: {
        backgroundColor: '#000'
    },
    HeaderRow: {
        height: 50,
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'flex-start',
        flexDirection: 'row',
        padding: 10
    },
    headerMenu: {
        height: 30,
        width: 30,
        display: 'inline-flex'
    }
});